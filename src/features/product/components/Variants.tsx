import React from 'react'
import { ProductModel } from '../../../data/productModel'
import { Box } from '@chakra-ui/react'

export default function Variants({}: ProductModel) {
  return (
    <Box>

    </Box>
  )
}