// import { Category } from "./category"

export type ProductModel = {
    thumbnail: string, 
    images: string[], 
    name: string, 
    price: number, 
    description: string, 
    percentOff: number, 
    brand: string, 
    brandLink: string, 
    category: string
}